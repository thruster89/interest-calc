<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Step1DetailMapper">

    <!-- ===============================
         INSERT
         =============================== -->
    <insert id="insert"
            parameterType="com.interestcalc.domain.Step1Detail">

        INSERT INTO STEP1_DETAIL (
              PLYNO
            , DEPOSIT_SEQ
            , DEPOSIT_DATE
            , PRINCIPAL
            , FACTOR
            , BALANCE
            , STEP1_END_DATE
            , DEDUCT_AMT
            , CREATED_AT
        ) VALUES (
              #{plyNo}
            , #{depositSeq}
            , #{depositDate}
            , #{principal}
            , #{factor}
            , #{balance}
            , #{step1EndDate}
            , #{deductAmt}
            , SYSDATE
        )

    </insert>

    <!-- ===============================
         DELETE (cleanup용)
         =============================== -->
    <delete id="deleteByPlyNo"
            parameterType="string">
        DELETE FROM STEP1_DETAIL
        WHERE PLYNO = #{plyNo}
    </delete>

</mapper>
